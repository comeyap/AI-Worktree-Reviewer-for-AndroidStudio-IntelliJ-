# The IntelliJ Platform already provides the Kotlin standard library, so let
# the Kotlin Gradle plugin skip adding its own stdlib dependency. This avoids
# the "stdlib may conflict with the version provided with the IntelliJ
# Platform" warning and keeps it out of the packaged plugin.
kotlin.stdlib.default.dependency=false
